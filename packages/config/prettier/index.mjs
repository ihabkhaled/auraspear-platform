/**
 * Canonical Prettier preset for AuraSpear packages.
 * Mirrors the formatting both apps already use.
 *
 * @type {import('prettier').Config}
 */
export default {
  semi: false,
  singleQuote: true,
  printWidth: 100,
  tabWidth: 2,
  trailingComma: 'es5',
  arrowParens: 'avoid',
  endOfLine: 'lf',
}
