{
  "name": "@auraspear/config",
  "version": "1.0.0",
  "private": true,
  "description": "Shared build/tooling presets (TypeScript, Prettier, ESLint, Tailwind) for the AuraSpear platform.",
  "type": "module",
  "exports": {
    "./tsconfig/base.json": "./tsconfig/base.json",
    "./prettier": "./prettier/index.mjs"
  },
  "files": [
    "tsconfig",
    "prettier",
    "eslint",
    "tailwind"
  ],
  "scripts": {
    "typecheck": "echo \"(config) presets only — nothing to typecheck\"",
    "lint": "echo \"(config) presets only\"",
    "lint:strict": "echo \"(config) presets only\""
  }
}
