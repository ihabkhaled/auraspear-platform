export enum AppLogSourceType {
  CONTROLLER = 'controller',
  SERVICE = 'service',
  GUARD = 'guard',
  INTERCEPTOR = 'interceptor',
  FILTER = 'filter',
  MIDDLEWARE = 'middleware',
  CRON = 'cron',
  JOB = 'job',
}
