export enum IntelIocSortField {
  LAST_SEEN = 'lastSeen',
  FIRST_SEEN = 'firstSeen',
  HIT_COUNT = 'hitCount',
  SEVERITY = 'severity',
  IOC_TYPE = 'iocType',
  IOC_VALUE = 'iocValue',
  SOURCE = 'source',
}
