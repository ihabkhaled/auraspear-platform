export enum KqlField {
  SEVERITY = 'severity',
  STATUS = 'status',
  AGENT = 'agent',
  AGENTNAME = 'agentname',
  HOST = 'host',
  RULE = 'rule',
  RULEGROUP = 'rulegroup',
  RULENAME = 'rulename',
  SOURCE = 'source',
}
