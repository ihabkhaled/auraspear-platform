// Side-effect CSS imports (e.g. `import './globals.css'`). tsc relies on the
// Next.js TS plugin for this; tsgo (typecheck:fast) needs the explicit ambient
// declaration. Additive — does not affect tsc.
declare module '*.css'
