export enum AttackPathSeverity {
  CRITICAL = 'critical',
  HIGH = 'high',
  MEDIUM = 'medium',
  LOW = 'low',
}

export enum AttackPathStatus {
  ACTIVE = 'active',
  MITIGATED = 'mitigated',
  RESOLVED = 'resolved',
}
