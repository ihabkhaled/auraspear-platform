export enum ConnectorType {
  WAZUH = 'wazuh',
  GRAYLOG = 'graylog',
  LOGSTASH = 'logstash',
  VELOCIRAPTOR = 'velociraptor',
  GRAFANA = 'grafana',
  INFLUXDB = 'influxdb',
  MISP = 'misp',
  SHUFFLE = 'shuffle',
  BEDROCK = 'bedrock',
  LLM_APIS = 'llm_apis',
  OPENCLAW_GATEWAY = 'openclaw_gateway',
}
