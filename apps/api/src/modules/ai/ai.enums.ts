export enum AiResponseModel {
  RULE_BASED = 'rule-based',
}

export enum AiProvider {
  BEDROCK = 'bedrock',
  LLM_APIS = 'llm_apis',
  OPENCLAW_GATEWAY = 'openclaw_gateway',
  RULE_BASED = 'rule-based',
}
