# pnpm workspace configuration
auto-install-peers=true
strict-peer-dependencies=false
# Keep the lockfile authoritative and reproducible across machines
resolution-mode=highest
# Hoist nothing implicitly beyond pnpm defaults; rely on explicit deps
shamefully-hoist=false
